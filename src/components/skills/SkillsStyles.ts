import { SxProps } from "@mui/material";

export const styles = {
  skillsBox: {
    display: "flex",
    flexDirection: "column",
    p: "10px 50px",
    color: "#fff",
    height: "90vh",
  },
  heading: {
    fontFamily: "Poppins",
  },
} satisfies Record<string, SxProps>;
